import { CodeBlock } from '@/docs/components'

# Introduction

Void UI is a personal React component library built on [Base UI](https://base-ui.com) and styled with [Tailwind CSS](https://tailwindcss.com). Design with a modern aesthetic for web applications.

> **Experimental**: This library is still in active development and primarily built for my personal projects. APIs may change without notice. Feel free to use it, but be aware of potential breaking changes.

## Why This Exists

I created this documentation mainly to help myself remember how to use these components across my projects. If you find it useful, feel free to use it in your own projects.

## Features

- **Accessible** - Built on Base UI with WAI-ARIA compliance
- **Customizable** - CSS variables and semantic color tokens
- **TypeScript** - Full type definitions
- **Phosphor Icons** - Integrated icon library

## Design Tokens

Void UI uses CSS custom properties (variables) for theming. Override these in your CSS to customize the look and feel.

### Semantic Colors

<CodeBlock
  code={`:root {
  /* Primary Actions */
  --void-primary: #22d3ee;
  --void-secondary: #a1a1aa;

  /* Status Colors */
  --void-success: #22c55e;
  --void-warning: #f59e0b;
  --void-danger: #ef4444;
  --void-info: #3b82f6;
}`}
  language="css"
  filename="app.css"
/>

### Surface & Borders

<CodeBlock
  code={`:root {
  /* Backgrounds */
  --void-bg: #fafafa;
  --void-surface: #ffffff;
  --void-bg-subtle: rgba(0, 0, 0, 0.02);
  --void-bg-muted: rgba(0, 0, 0, 0.04);

  /* Borders */
  --void-border: rgba(0, 0, 0, 0.08);
  --void-border-hover: rgba(0, 0, 0, 0.15);

  /* Text */
  --void-text: #09090b;
  --void-muted: #71717a;
}`}
  language="css"
  filename="app.css"
/>

### Shadows

<CodeBlock
  code={`:root {
  --void-shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.05);
  --void-shadow-md: 0 4px 6px rgba(0, 0, 0, 0.08);
  --void-shadow-lg: 0 10px 15px rgba(0, 0, 0, 0.1);
}`}
  language="css"
  filename="app.css"
/>

## Customization

### Overriding Colors

Create a custom theme by overriding CSS variables in your global stylesheet:

<CodeBlock
  code={`/* Custom brand colors */
:root {
  --void-primary: #8b5cf6;
  --void-success: #10b981;
}

/* Dark mode overrides */
[data-theme="dark"], .dark {
  --void-bg: #09090b;
  --void-surface: #18181b;
  --void-text: #fafafa;
  --void-muted: #a1a1aa;
  --void-border: rgba(255, 255, 255, 0.08);
}`}
  language="css"
  filename="app.css"
/>

### Component-Level Customization

All components accept a `className` prop for Tailwind overrides:

<CodeBlock
  code={`<Button className="bg-purple-500 hover:bg-purple-600">
  Custom Button
</Button>

<Card className="border-2 border-dashed">
  Custom Card
</Card>`}
  language="tsx"
/>

### Extending with Variants

Use the `cn()` utility to conditionally apply classes:

<CodeBlock
  code={`import { cn } from '@/lib/utils'

<Badge className={cn(
  isNew && 'animate-pulse',
  isPriority && 'ring-2 ring-offset-2'
)}>
  Label
</Badge>`}
  language="tsx"
/>

## Components

40+ components across several categories:

- **Layout** - Stack, Separator, Card
- **Forms** - Button, Input, Checkbox, Switch, Select, Slider, Radio
- **Display** - Badge, Avatar, Alert, Table, Progress
- **Navigation** - Tabs, Accordion, Pagination, Menu, Dropdown
- **Overlay** - Dialog, Popover, Tooltip, Toast
- **Typography** - Heading, Text, Label, Kbd

## Getting Started

Head over to the [Quick Start](/docs/quick-start) guide to get started.
